marks = [54, 96, 85, 45];
console.log("Before Operations : "+marks);
// marks[0] = 71  // updates the element using the index number
// marks.pop()   //removes last element on the array
// marks.push(2)  //added new element at the end of the array
// console.log("Join Method : "+marks.join(""));  //joins the array element
// console.log("Converted into String : "+marks.toString());   //convert the array elements to string
// marks.shift()     //removes the first element
// marks.unshift(12)  //addes new element at the first
// console.log(marks.length);  //prints the lenght
// delete marks[1];  //removes the deletes the element and perserve the empty space
// console.log(marks.sort());  //sort from smallest to largest
// marks.splice(1, 1, 86); // this method removes the element of specified index number and than we specify number of element to remove and then we add new elements

console.log("After  Operations : "+marks);
